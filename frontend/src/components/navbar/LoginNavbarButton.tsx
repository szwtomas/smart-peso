import { Button } from "@nextui-org/react";

export function LoginNavbarButton() {
  return (
    <Button variant="bordered" radius="sm">
      <p className="text-white">Log In with Google</p>
    </Button>
  );
}
